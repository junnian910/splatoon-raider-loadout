# Local editor and operating-system files
.DS_Store
Thumbs.db
Desktop.ini

# Node package caches and generated local artifacts
node_modules/
.wrangler/
.dev.vars
.env
.env.*

# Browser-exported copies; the tracked catalog is the project baseline.
splatoon-raider-catalog-v5 (1).json

# 设计草稿与空文件（不提交）
曲面输入
手机端菜单调整.txt
片头动画设计方案.md
