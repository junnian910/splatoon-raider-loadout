# Splatoon Raider 配装器 — 交接文档

> 本文是当前项目的完整交接记录，不只是功能摘要。后续修改前请先阅读“现状总览”“交互规则”和“已知问题”三部分。

## 项目概述

Splatoon 主题的游戏配装（Build）编辑器，纯前端 vanilla JS 单页应用，无框架、无构建工具，直接双击 `index.html` 即可运行。所有数据存储在浏览器 localStorage 中。

项目目标是让用户依次选择背包、武器、配件、零件和秘宝，并在最终展示页查看完整配装。项目没有 npm 构建链，所有页面共享 `styles.css`、`data.js` 和 `core.js`。

## 目录与页面入口

| 文件/目录 | 用途 |
|---|---|
| `index.html` | 主页、六张入口卡、左侧步骤轮盘、挂坠和五步编辑器 |
| `app.js` | 主编辑器渲染、选择交互、主题同步、分享/导入、动画和主页特效 |
| `core.js` | 数据存储、数据规范化、规则校验、分享编码、DOM 工具、确认弹窗、Canvas 粒子 |
| `data.js` | VERSION 5 种子数据、背包、技能、品质边框、秘宝、武器类型和默认配置 |
| `library.html` / `library.js` | 零件数据库、筛选、编辑、软删除、批量添加、JSON 导入导出 |
| `weapon-library.html` / `weapon-library.js` | 武器类型和武器记录管理、图片上传压缩、软删除 |
| `treasure-library.html` / `treasure-library.js` | 秘宝记录管理、普通/背包归属、图片上传压缩、软删除 |
| `showcase.html` / `showcase.js` | 最终配装展示，读取同源 localStorage |
| `styles.css` | 全站布局、主题、组件状态、动画、响应式和性能覆盖层 |
| `tests/rules.test.js` | 核心规则单元测试 |
| `splatoon-raider-catalog-v5.json` | 当前导出的数据库文件示例/备份，不会自动替代浏览器缓存 |

图片和素材目录：

- `背包照片/`：首次展示图、速度/力量/技巧背包正反面。
- `零件外观图片/`：白、绿、紫、金、彩色五种品质边框，均按 512×512 设计。
- `插件图片/`：零件截图素材；`插件图片裁剪/` 保存个别裁剪参考。
- `挂坠图片/`：`正面.jpg` 和 `反面.jpg`。
- `GB7.ttf`、`GB9.ttf`：本地游戏风格字体。
- 根目录 `*.txt`：镜面按钮、菜单、绳子、粒子背景、磁铁按钮、渐变文字、卡片悬浮、彩色按钮、动画等参考文件。

## 现状总览

- 主编辑器和三个数据管理库均已可用。
- 数据版本为 VERSION 5，运行时数据写入浏览器 localStorage。
- 页面视觉已经从早期深色主题调整为白色/银灰磨砂玻璃体系，并使用深色文字和局部主题色。
- 首页、步骤页、库页面和最终展示页都加入了不同程度的动画与主题样式。
- 挂坠目前使用本地 DOM 版本，不再加载联网 Rapier/物理模块。
- 当前最大遗留问题是：秘宝、零件等大量卡片在切换页面时仍可能出现短暂卡顿。

## 已完成的模块

### 1. 主页（index.html）
- 大标题"斯普拉遁涂击队配装"，渐变流动文字效果
- 6 张 MagicBento 风格导航卡片，带聚光灯、粒子星效、边框发光、点击涟漪
- 从下方依次滑入的入场动画
- 左侧弧形轮盘导航（倾斜 30°/步），主页和配装器都能用

主页具体改动：

- 保留 6 张入口卡：背包、武器、配件、零件、秘宝、最终展示。
- 入口卡支持聚光灯跟随鼠标、边框发光、点击涟漪和悬浮上移。
- 背景有 Canvas 粒子层，当前为金色粒子；粒子数量和绘制频率已针对性能降低。
- 主页右侧挂坠只在主页显示，进入步骤页时隐藏，返回主页时恢复。
- 首页入口卡不再隐藏“选择背包”卡片。
- 主页标题和说明使用较宽的排版，避免窄屏出现过多换行。

### 2. 配装编辑器（5 步骤）
| 步骤 | 内容 | 状态 |
|------|------|------|
| 背包 | 大图→鼠标移入/2.5s后分裂为三张可翻转卡片，选中后保存状态，按钮配色跟随 | ✅ |
| 武器 | 11 类型左右切换弹窗，下方显示该类型武器列表 | ✅ |
| 配件 | 三列技能选择器，名称+图片 | ✅ |
| 零件 | 同名分组平铺网格，品质边框叠加，已安装排序 | ✅ |
| 秘宝 | 卡片式选择安装 | ✅ |

编辑器的固定流程是：主页入口/轮盘 → 背包 → 武器 → 配件 → 零件 → 秘宝 → 最终展示。每一步由 `showPanelOnly()` 控制单独显示；进入步骤时会滚动到页面顶部，并给当前步骤中的组块添加顺序进入类。下一步按钮的 `data-next` 与轮盘的 `data-step` 使用同一套步骤 ID。

编辑器公共能力：

- `保存配置`：每次选择背包、技能、零件、秘宝、武器或武器效果后写入 v5 build localStorage。
- `导出配置`：生成包含当前 build、当前用到的技能/零件/秘宝/武器定义和图片 Data URL 的分享码或 JSON。
- `导入配置`：支持分享码、粘贴 JSON 和选择 JSON 文件；导入前校验版本、应用标识、大小和规则。
- `重置配置`：清理当前 build，保留 catalog 数据。
- 底部 Dock：主页、零件库、武器库、秘宝库，以及主页中的导入/导出入口。
- Toast 与确认弹窗：删除操作使用软删除和确认提示，不直接从数据库数组物理删除。

#### 背包选择

- 背包有正面卡和反面卡，三种背包分别读取 `背包照片/` 中的图片。
- 未选择时先显示大图，鼠标移入或等待约 2.5 秒后分裂为三张卡片。
- 卡片点击后翻转并选中，其他背包卡恢复未选状态。
- 选中背包会保存到 `build.mainPackId`，并切换对应的速度/力量/技巧主题色。
- 每个背包的“重新选择”按钮为白底深色字，并有对应色荧光。
- 背包卡支持类似 TiltedCard 的鼠标倾斜：旋转幅度约 30°，悬浮缩放约 1.10。
- 背包区域后方的大块白色背景已移除，只保留卡片本身。

#### 武器选择

- 武器选择使用弹窗，不是页面内无限展开。
- 弹窗上方可以左右切换武器类型，下方显示当前类型的武器列表。
- 武器卡有普通、悬浮、选中三种状态。
- 弹窗已从旧深色方案改为浅色磨砂玻璃，标题使用深色科技渐变，正文和武器说明使用深色高对比文字。
- 武器卡选中后写入 `build.weaponId`，武器效果写入 `build.weaponEffectIds`。
- 规则限制：没有武器时不能安装武器效果，效果最多 3 个且不能重复。

#### 配件/技能选择

- 配件选择为三列技能选择器，分别对应速度、力量、技巧。
- 同一个技能不能重复选择；当前选中技能会在零件页顶部显示为技能标签。
- 技能标签已改为居中排列。
- 技能弹窗同样使用浅色磨砂玻璃和深色文字。
- 技能卡、技能标签和技能槽都有选中状态。

#### 零件选择

- 零件按技能归属显示，并按名称分组。
- 同名零件属于同一类型，只能互相切换，不能同时安装多个同名零件。
- 同类型排序优先级：彩色/彩虹 → 金色 → 紫色 → 绿色 → 白色，再按成本数字从高到低。
- 桌面端目标为一行 8 个零件，移动端根据屏幕缩减为 4 个。
- 零件品质边框来自 `零件外观图片/`，边框尺寸为 512×512。
- 零件图片来自插件图片/自定义图片，插件背景使用黑色科技风、网格纹理和冷色光晕。
- 零件卡已安装时增加主题色外框、白色描边和发光提示。
- 右侧“已安装零件”面板会显示当前成本、槽位、品质边框、零件图和加成。
- 右侧零件图的显示区域已从较小比例扩大，使图片更贴近品质边框。
- 鼠标移到零件时显示详细悬浮提示，但提示内容只保留加成数值，例如 `+40%`，不显示名称或前缀。
- 悬浮提示位置已多次下移，目标是贴近零件卡片。
- 当前加成数值统一使用深色高对比文字。
- 为减少卡顿，零件未使用高开销 3D 悬浮；保留必要的提示、选中和点击交互。

零件卡的 DOM 结构为：`plugin-tile-button` → 品质边框图 `plugin-tile-border` → 零件图 `plugin-tile-part-img` → 成本角标 `plugin-tile-cost`。右侧已安装项使用对应的 `installed-thumb` 叠层。不要改回 button 内嵌复杂 div 的旧结构，也不要把边框图片放到会影响点击区域的伪元素上。

零件渲染流程：先取当前技能的 active plugins，按名称建立 `Map` 分组；每组内调用 `sortPlugins()`。`pluginQualityKey()` 将品质图片路径映射为 `rainbow/gold/purple/green/white`，排序权重为彩虹 0、金 1、紫 2、绿 3、白 4，再按 `slotCost` 降序和 id 排序。安装时先找出同名已装 id，使用投影 build 做成本校验，再替换同类型零件。

#### 秘宝选择

- 秘宝按普通秘宝和背包专属秘宝分组显示。
- 最多安装 5 个秘宝。
- 当前背包专属秘宝可以安装，跨背包专属秘宝受规则限制。
- 跨背包专属秘宝最多允许 1 个。
- 秘宝卡支持已安装、不可用、悬浮状态。
- 页面切换时秘宝组会按块进入，但大量数据仍可能造成短暂卡顿，这是当前主要遗留问题。

秘宝选择调用 `rules.canInstallTreasure()`：普通秘宝可用；当前背包专属秘宝可用；相邻/跨背包专属最多安装一个；不属于当前背包或相邻背包的专属秘宝不可用；总数量最多五个。点击已安装项会移除它，点击不可用项只提示原因，不修改 build。

### 3. 数据管理页面
- **零件库** (`library.html`)：表格+弹窗 CRUD，品质筛选，复合图片预览
- **武器库** (`weapon-library.html`)：按武器类型分组管理
- **秘宝库** (`treasure-library.html`)：卡片式管理

三个库的共同改动：

- 页面从黑白旧样式改为白色磨砂玻璃体系。
- 零件库使用紫粉雾彩主题，武器库使用蓝青主题，秘宝库使用暖金主题。
- 顶部大标题使用对应主题的流动渐变文字，动画周期已放慢以降低开销。
- 顶部操作按钮统一为圆角胶囊格式，使用同一套紫 → 粉 → 蓝 → 青渐变，并带轻微边缘发光。
- 表格表头使用对应库主题渐变，鼠标移动到行时有 ChromaGrid 风格的局部聚光和边缘强调。
- 库页面按钮悬浮时使用深色前景，避免浅色背景上的文字消失。
- 零件库支持搜索、配件筛选、品质筛选、状态筛选、单个添加、批量添加、导入和导出。
- 武器库支持搜索、武器类型筛选、状态筛选和编辑。
- 秘宝库支持搜索、归属筛选、状态筛选、添加和编辑。

数据库管理细节：

- 零件单条编辑字段：所属配件、名称、成本、品质、效果说明、加成数字、图片。图片上传前用 Canvas 压缩为最多 512px 的 WebP Data URL，限制原文件 5MB。
- 零件批量添加共享所属配件、名称、效果说明、图片；每一行独立填写成本、品质、加成数字；提交时为每行生成唯一 id。
- 武器编辑字段：武器类型、名称、描述、图片；武器图片同样压缩到最多 512px。
- 秘宝编辑字段：名称、描述、普通/背包专属归属、图片；同名活动秘宝不允许重复保存。
- 删除均为写入 `deletedAt` 的软删除；列表可切换“已删除”状态并执行恢复。
- 运行时修改优先保存到 `splatoon-raider-v5-catalog`；数据库文件按钮使用 File System Access API（浏览器支持时）或下载 JSON 作为回退。
- 编辑器实际读取的是 localStorage，不会自动读取根目录 JSON；更换数据库文件后必须在零件库点击导入，或清除缓存后重新载入种子。

### 4. 主题与视觉
- 当前为白色/银灰磨砂玻璃主题，深色文字和主题色强调
- 速度/力量/技巧使用蓝青、橙色、紫色主题变量
- Canvas 粒子背景当前为金色；桌面约 180 个、移动端约 90 个，约 60fps 上限，页面隐藏时暂停
- 底部 Dock 为白色玻璃质感，悬浮时使用深色前景
- 左侧弧形轮盘使用深色彩色文字，并保存当前滚轮索引
- 库页面标题使用对应主题渐变文字，顶部按钮使用统一紫粉蓝青渐变
- 武器/技能弹窗为浅色磨砂玻璃，文字使用深色高对比
- 零件卡片使用黑色科技风底板、网格纹理和冷色光晕
- 全局 GB7 字体，抗锯齿

视觉实现来源和取舍：

- `渐变文字.txt` / `大标题闪.txt`：用于主页和库页大标题的渐变流动文字；颜色以库的主题为主，周期已放慢。
- `彩色按钮.txt` / `镜面按钮.txt`：用于库页胶囊按钮和编辑器下一步按钮的渐变、镜面扫光及主题变体。
- `卡片悬浮.txt`：背包卡的 TiltedCard 参考，当前旋转幅度 30、悬浮缩放 1.10；零件卡不使用同等高开销悬浮。
- `左侧步骤滚轮.txt` 与用户参数：字号 3rem、间距 1.4、curve 1、tilt 6deg、blur 2px、fade 0.25、smoothing 200ms、inset 80px；当前是从中间向外的半圆布局，隐藏时整体向左。
- 用户提供的 SplitText/BlurText/ChromaGrid 代码没有直接引入 React 依赖，而是转为原生 CSS/JS 的分块进入、顺序文字显示、表格聚光和主题渐变。
- `minimalist-ui` 的约束用于控制大面积颜色和过重阴影；`high-end-visual-design` 的双层容器、主题局部光和 transform-only 动效用于提升层次；性能约束优先于继续叠加滤镜。

主题变量主要集中在 `styles.css` 的 CSS 变量层：

- `--ink`：主文字颜色
- `--ink-soft`：次要文字颜色
- `--surface`：磨砂面板背景
- `--line`：边框颜色
- `--theme-accent`：当前背包主题色
- `--theme-accent-soft`：当前背包浅色背景
- `--theme-glow`：当前背包发光色

当前背包主题：

| 背包 | 主色方向 | 作用 |
|---|---|---|
| 速度 | 蓝青 | 技能标签、零件面板、下一步按钮 |
| 力量 | 橙红 | 技能标签、零件面板、下一步按钮 |
| 技巧 | 紫色 | 技能标签、零件面板、下一步按钮 |

三个库主题：

| 页面 | 标题/表头主题 |
|---|---|
| 零件库 | 紫、玫红、淡粉 |
| 武器库 | 蓝、青、浅青 |
| 秘宝库 | 棕金、暖金、米金 |

### 5. 数据模型（VERSION 5）
- Packs: speed/power/technique，各有 5 个技能
- Seed 数据：~40 个测试零件 + 11 个测试武器
- 自定义数据通过 localStorage 持久化

主要字段：

- `build.mainPackId`：主背包；`build.packStates`：每个背包独立保存的技能、零件、秘宝和武器状态。
- `build.skillIds`：最多三个技能 id；`build.pluginsBySkillId`：按技能 id 存放零件 id 数组。
- `build.treasureIds`：秘宝 id 数组；`build.weaponId`：当前武器；`build.weaponEffectIds`：最多三个效果 id。
- catalog 中的 `skills/plugins/treasures/weapons/weaponTypes/weaponEffects` 记录都支持 `deletedAt`。品质字段存储的是 `seed.qualityBorders` 中的图片路径，不是显示名称。
- `normalizeCatalog()` 会用种子补齐缺失数组；`normalizeBuild()` 会把旧 build 规范化为三技能槽、五秘宝槽和三效果上限，并为三个背包建立 `packStates`。

## 当前卡点

### 性能与动画
- 页面切换已加入顺序入场动画，但秘宝/零件等大量卡片同时创建时仍可能出现短暂卡顿。
- 已降低背景粒子数量、限制粒子绘制频率、页面不可见时暂停；尚未做虚拟列表或分帧 DOM 创建。
- 后续优先考虑分批 `requestAnimationFrame` 创建卡片、减少 `backdrop-filter` 层、避免一次性 `replaceChildren` 大数组。

### 当前代码中的性能结构

- 主页聚光在 `app.js` 的 `initBentoFx()` 中维护一个全局鼠标监听，但事件只保存坐标；实际处理放到单个 `requestAnimationFrame`。卡片 `getBoundingClientRect()` 结果在初始化、滚动、resize 时缓存。
- 按钮磁吸已经改为 `.specular-btn` 自身的 `pointermove/pointerleave`，通过 `--magnet-x/--magnet-y` 和 GPU transform 移动。
- 粒子 Canvas 在 `core.js` 中单独运行，使用 `pointerPending` 延迟读取鼠标位置；页面 hidden 时停止 requestAnimationFrame。
- `styles.css` 末尾的最终覆盖层对滚动内容关闭 `backdrop-filter`，并给主要卡片设置 `contain: layout paint`。后续整理 CSS 时必须保留这些规则的语义，不要只删除“看起来重复”的最后覆盖层。
- 目前最明显的一次性工作仍是 `renderPlugins()` 和 `renderTreasures()` 的 `replaceChildren(...map(...))`。如果继续优化，应分批创建组或采用文档片段，确保事件绑定和排序保持不变。

已经做过的性能处理：

- Canvas 粒子从早期 600 个降低为桌面约 180 个、移动端约 90 个。
- 粒子绘制频率由约 30fps 调整为接近 60fps，但保留低粒子数量。
- 页面切到后台时暂停粒子动画，重新回到页面时恢复。
- Canvas 分辨率受设备像素比上限限制，避免高分屏过度放大。
- 主页全局鼠标聚光更新改为 `requestAnimationFrame` 节流，每帧最多处理一次。
- 首页悬浮星尘数量从 12 个降至 6 个。
- 库页面标题渐变从 8 秒放慢到 12 秒，表头渐变从 9 秒放慢到 14 秒。
- 挂坠联网物理模块已移除，避免 CDN/WASM/物理循环造成额外负载。

仍然存在的问题：

- 秘宝、零件等列表即使有顺序动画，DOM 仍会在进入页面时一次性创建。
- 多层 `backdrop-filter`、表格聚光和页面入场动画叠加时，低性能设备仍可能掉帧。
- 下一次性能优化应优先进行分帧 DOM 创建，而不是继续增加动画参数。

### 挂坠
- 已移除 `lanyard-physics.js` 的 CDN/Rapier 联网加载，避免网络等待和持续物理开销。
- 当前仅使用本地 DOM 挂坠，支持拖拽摆动和正反面翻转，仅主页显示。
- 正面图片：`挂坠图片/正面.jpg`。
- 反面图片：`挂坠图片/反面.jpg`。
- 两张图通过固定比例和 `object-fit: cover` 统一显示尺寸。
- 当前拖拽是轻量级横向摆动，不是 Rapier 真物理。

挂坠交互细节：

- DOM 结构由绳子、连接环、卡片容器和正反两张图片组成；绳子固定在视口顶部，卡片位于页面右侧。
- `pointerdown` 记录起点并调用 `setPointerCapture()`；拖动超过 4px 才认为是拖拽，横向位移换算为 -18° 到 18° 的摆动。
- `pointerup/pointercancel/pointerleave` 会释放拖拽并把摆角回到 0；拖拽后抑制同一次 pointer 的 click，避免误翻面。
- 普通点击只切换 `.is-flipped`，通过 `rotateY(180deg)` 在正反面之间翻转。
- 进入任意 builder 步骤会设置 `hidden` 隐藏挂坠；返回主页重新显示。挂坠 CSS 保留了未来物理 canvas 的兼容类名，但当前没有物理脚本或联网依赖。

### 最终展示页
- `showcase.js` 从 localStorage 读取当前配置并渲染技能、零件、秘宝和武器摘要。
- 已加入渲染异常兜底；若显示为空，检查是否使用同一个 HTTP 源访问编辑器和展示页，以及 localStorage 是否被清理。
- 展示页不是独立数据库：它只读取 `splatoon-raider-v5-catalog` 和 `splatoon-raider-v5-build`。从 `file://` 编辑器跳到 HTTP 展示页，或更换端口/域名后，两页会使用不同 localStorage，表现为“最终展示为空”。
- 页面入口卡和底部 Dock 使用相对链接；部署到子目录时保持所有页面与素材处于同一目录层级。
- 渲染顺序为三张技能卡 → 背包摘要 → 秘宝摘要 → 武器摘要；若脚本异常，页面会在状态区域提示“配置读取失败，请返回编辑器重新保存一次”。

### 左侧轮盘
- 滚轮索引保存在 `#stepWheel` 的 `data-current-index` 中，并由主页卡片、下一步按钮、点击和滚轮入口统一同步。
- 若再次出现跳回开头，检查是否有代码直接调用 `updateWheel(0)` 或绕过 `syncWheelIndex()`。

轮盘参数来源于用户给出的 OptionWheel 参考：

- 文字颜色：`#a6a6a6`
- 激活颜色：`#ffffff`（后续改为深色彩色文字以适配浅色背景）
- 字号：`3rem`
- 间距：`1.4`
- Curve：`1`
- Tilt：`6deg`
- Blur：`2px`
- Fade：`0.25`
- Smoothing：`200ms`
- Inset：`80px`
- 当前采用从中间向外的半圆布局，隐藏时整体向左收缩。

轮盘实现注意：

- `updateWheel(activeIndex)` 根据相邻项目距离计算弧线位置、X/Y 偏移和旋转角，使用 CSS 变量 `--wheel-x`、`--wheel-y`、`--wheel-rot`，不修改布局属性。
- `syncWheelIndex(stepId)` 是唯一推荐的状态同步入口；它同时更新内存索引、`data-current-index` 和视觉位置。主页卡、下一步按钮、点击和滚轮事件都应该调用它。
- 隐藏时通过 `.step-wheel:not(:hover):not(:focus-within)` 设置 `visibility/opacity/pointer-events`，不要将项目删除或移动到另一个容器，否则会破坏“显示时位置不移动”的要求。
- 当前 CSS 中存在早期主题规则和末尾最终覆盖规则，修改轮盘颜色时优先修改最后一组 `.step-wheel-item` 规则并保留深色高对比文字。

### 数据保存说明（已补充）
- 运行时 catalog 仍保存在浏览器 `localStorage`（键名 `splatoon-raider-v5-catalog`），这是浏览器页面可直接写入的地方。
- 零件库新增“保存数据库文件”：优先使用浏览器文件选择器把完整 catalog 保存为 `splatoon-raider-catalog-v5.json`；不支持时会下载 JSON，需要手动放入项目文件夹。
- 零件库新增“从文件导入”：选择此前保存的 v5 JSON，导入后会同时写入当前浏览器缓存。
- 浏览器安全策略不允许网页在用户未授权时静默改写项目文件夹，因此不能做到每次点击“保存零件”都自动修改本地文件。
- 批量添加零件：所属配件、零件名称、效果说明、零件图片为共享字段；成本、品质、加成数字按行独立选择。

### 数据迁移与恢复流程

1. 修改前先在零件库点击“保存数据库文件”，得到 v5 JSON 备份。
2. 若只想恢复配装，不要导入整个数据库；在主页使用“导入”并应用 build 分享码或 JSON。
3. 若需要恢复零件、武器或秘宝定义，在对应库或零件库使用“从文件导入”；导入会合并同 id 记录并写入 localStorage。
4. 不能把 `splatoon-raider-catalog-v5.json` 直接改名为脚本或覆盖 `data.js`；种子数据和运行时 catalog 是两层不同来源。
5. 不要修改 VERSION 5，除非明确设计迁移脚本；版本号变化会让 `normalizeCatalog()` 和 `normalizeBuild()` 回退到种子/默认配置。

### 已解决的关键 Bug
1. `el()` 函数的 `replaceChildren` 数组未展开 → 产生 `[object HTMLDivElement]` 乱码。**已修**：加了递归展平 + 展开运算符。
2. `button > div` 非法嵌套 → 浏览器 DOM 修正导致乱码。**已修**：改为纯 button 结构。
3. `file://` 协议下 WebGL 加载本地图片失败（CORS）。**无解**：需用 HTTP 服务器（`npx serve .`）。
4. Canvas 被 file:// 图片污染 → WebGL 拒绝渲染。**绕过**：用程序化纹理代替。
5. `app.js` 多余的 `}` 导致整个 IIFE 提前关闭。**已修**。
6. 背包图片超出边框、正反面图片比例不同。**已修**：固定卡片比例、`object-fit` 和内边距。
7. 插件图片与品质边框融合不自然。**已修**：图片缩小/放大规则、黑色科技背景和边框叠层。
8. 同名零件可以重复安装。**已修**：同名类型互斥切换。
9. 零件排序不符合品质优先级。**已修**：彩虹、金、紫、绿、白，再按成本降序。
10. 右侧已安装零件面板过窄。**已修**：扩大布局并调整缩略图显示区域。
11. 主题切换后按钮和零件面板颜色不连续。**已修**：下一步按钮、零件面板、技能标签同步当前背包主题。
12. 库按钮悬浮时文字看不清。**已修**：浅色普通态、深色悬浮态和高对比文字。
13. 最终展示页可能因渲染异常整页空白。**已修**：增加渲染异常兜底提示。
14. 左侧轮盘滚轮索引偶尔回到开头。**已增加**：统一同步入口和 `data-current-index` 状态记录，但仍需真实浏览器继续观察。

15. 鼠标移动导致主页聚光、按钮磁吸和粒子同时竞争帧。**已修**：聚光坐标 rAF 节流、磁吸改为局部 pointer 事件、粒子延迟读取坐标。
16. 零件页浅色外层和深色科技卡片反差突兀。**已修**：增加浅灰蓝网格工作台、分组内层和主题局部光晕，避免整块面板高饱和渐变。
17. 滚动内容上的多层磨砂滤镜造成 GPU 重绘。**已修**：保留固定头部/弹窗/Dock 的必要模糊，滚动卡片和数据表关闭 `backdrop-filter`，并增加局部 paint containment。
18. 主页聚光滚动后位置可能滞后。**已修**：在 scroll/resize 时用单个 rAF 更新缓存矩形。

### 未完成的体验优化
- 武器库数据录入功能已有，但选择弹窗中切换类型时，子武器列表需要用户提前录入
- 零件批量录入已删除独立页面，需评估是否重新集成
- 真实浏览器 Performance 面板验证尚未完成；Node 语法检查不能替代 FPS、长任务和图片解码检查。
- 低性能设备上秘宝/零件首次进入仍可能有短暂卡顿，下一步应做分帧 DOM 创建或虚拟化，而不是继续增加悬浮动画。
- `styles.css` 仍有历史主题覆盖层，功能稳定后可做一次无视觉变化的 token 合并；合并前必须保留末尾最终覆盖规则。

## 绝对不要踩的坑

1. **replaceChildren 必须展开数组**：`el.replaceChildren(...arr)` 不能写成 `el.replaceChildren(arr)`
2. **button 内不能嵌套 div**：浏览器会"修正"导致 DOM 结构错乱
3. **file:// 协议限制**：WebGL/Canvas 无法加载本地图片，必须 HTTP 服务
4. **app.js 代码改动要小心**：主 IIFE 很长（~900行），括号匹配容易出错
5. **el() 函数的 text 参数必须传字符串**：虽然加了 `String()` 保护，但传 DOM 元素仍会显示 `[object HTMLXXX]`
6. **VERSION 升级意味着清空数据**：改版本号前确保用户知情

## 文件结构

```
├── index.html          # 主页 + 配装编辑器
├── library.html        # 零件库
├── weapon-library.html # 武器库
├── treasure-library.html # 秘宝库
├── showcase.html       # 最终展示
├── data.js             # 种子数据 + VERSION
├── core.js             # 存储/规则/工具函数/粒子背景
├── app.js              # 主编辑器逻辑（~900行）
├── library.js          # 零件库逻辑
├── weapon-library.js   # 武器库逻辑
├── treasure-library.js # 秘宝库逻辑
├── showcase.js         # 展示页逻辑
├── styles.css          # 全局样式（持续追加覆盖规则，已超过 1900 行）
├── tests/rules.test.js # 核心规则单元测试
├── 背包照片/           # 背包相关图片（8张）
├── 零件外观图片/       # 品质边框 PNG（5张）
├── 挂坠图片/           # 正反面卡图（2张）
├── 插件图片/           # 截图素材
├── GB7.ttf             # 游戏字体
└── *.txt               # 参考设计文件
```

## 如何运行

```bash
# 本地开发（推荐使用 HTTP，图片和 localStorage 行为更稳定）
npx serve .
# 然后打开 http://localhost:3000

# 运行测试
node tests/rules.test.js
```

### 推荐验收清单

- 使用同一个 HTTP 源打开 `index.html` 和 `showcase.html`，选择至少一个背包、三个配件、若干零件、秘宝和武器后打开最终展示。
- 在零件页确认同名零件切换不会重复，品质顺序为彩虹→金→紫→绿→白，同品质成本从高到低。
- 在零件页移动鼠标，确认提示只显示加成数字；点击已安装零件可取消安装；成本不得超过 40，插槽不得超过 9。
- 在秘宝页确认最多 5 个、跨背包专属最多 1 个；在武器页确认效果最多 3 个且无武器时禁用。
- 返回主页确认挂坠可点击翻面、可横向拖拽摆动，进入步骤页后挂坠隐藏。
- 鼠标移到左侧轮盘，确认所有步骤在原位置显示；离开后全部隐藏且轮盘整体靠左。
- 浏览器控制台无脚本错误；`node --check` 和规则测试通过。

## 本轮优化记录（2026-08-03）

### 零件录入弹窗静态模式（追加）

- “添加零件”和“批量添加”两个弹窗现在使用 `body.plugin-editor-open` 性能模式。
- 打开任一弹窗时暂停 Canvas 金色粒子并清空画布；关闭时通过 dialog `close` 事件恢复粒子。
- 弹窗内部禁用入场动画、镜面扫光、渐变过渡、悬浮抬升、滤镜和额外阴影，改用固定浅色背景、单层边框和深色文字。
- 品质选择只保留静态边框、选中描边和基础 hover 背景；保存、取消、关闭按钮不再移动或发光。
- 该模式只作用于零件库的 `#pluginDialog` 和 `#batchPluginDialog`，武器弹窗、秘宝弹窗、主编辑器和普通库页面不受影响。

### 零件选中态与背景（追加）

- 零件选择工作区和零件卡内部均改为白色背景，仅保留非常低对比的网格线，避免黑色底板干扰零件图片。
- 已安装零件现在只使用一圈主题色单边框表示选中，取消此前的外轮廓 + 内描边 + 发光叠加；右下角保留“已选”角标作为文字提示。
- 选中态通过 `plugin-tile-button.is-installed::after` 和 `plugin-selected-mark` 实现；品质边框图片仍然保留，但只表示品质，不再作为第二层选中提示。

### 鼠标移动与帧率

- 合并主页聚光与按钮磁吸的实时处理：聚光只使用一个 `requestAnimationFrame` 调度器，鼠标事件只记录坐标，不再在每次事件中读取所有卡片布局。
- 聚光卡片的位置矩形在初始化、滚动和窗口缩放时缓存，并通过单帧调度刷新，避免 `getBoundingClientRect()` 热路径造成强制回流。
- 磁吸改为按钮自身 `pointermove`，离开后通过 CSS 变量归零；切换到零件页时不再为隐藏按钮做全局计算。
- 粒子 Canvas 的鼠标坐标改为先采样、绘制帧内读取；Canvas 继续限制 DPR、粒子数量，并在页面不可见时暂停。
- 滚动内容（工作区、卡片网格、表格等）关闭大面积 `backdrop-filter`，保留固定头部、Dock 和弹窗的必要模糊，减少 GPU 重绘。
- 零件卡片和主要列表增加 `contain: layout paint`，将局部重绘限制在组件内部；动画仍只改变 `transform`、`opacity` 和 CSS 变量。

### 零件选择区重新设计

- `catalog-pane` 改为浅灰蓝“零件工作台”：低对比网格纹理、局部主题光晕和内层分组卡片，避免白色外框与黑色零件图直接冲突。
- 每个同名零件组使用独立的轻量双层结构，组标签、间距和分隔线统一；深色科技风只保留在零件缩略图内部。
- 速度、力量、技巧主题只在工作台局部光晕、标题边线、已安装高亮中体现，不再把整块面板刷成高饱和渐变。
- 已安装面板改为稳定的浅色信息面板，保留主题色成本线和选中零件发光。

### 验证

- 已通过 `node --check app.js`、`node --check core.js`、`node --check showcase.js`。
- 已通过 `node tests/rules.test.js`（all assertions passed）。

## 历史需求实现对照

这一节记录项目从最初视觉调整到当前版本的主要决策，后续接手时不要把已经确认过的取舍重新改回去。

### 图片、卡片和布局

- 背包正面/反面图片统一放进固定比例卡片，使用 `object-fit` 和内边距，避免图片超过边框、两面尺寸不一致或出现缩放重影。
- 零件品质边框与零件图均按 512×512 资产处理；零件图通过内层比例缩放，右侧已安装缩略图扩大并贴近品质边框。
- 零件同名记录归为同一类型，只允许切换；已安装状态用主题色边框、白色描边和发光标记。
- 零件桌面端每行目标 8 个，窄屏使用媒体查询缩减列数；右侧已安装面板扩宽，成本和 `/40` 在同一行显示。
- 成本数字、加成数字和悬浮提示均改为深色高对比；悬浮提示最终只保留加成数值，不带名称或前缀，并放在零件卡下方较近的位置。
- 背包选择未完成时显示首次展示大图；鼠标移入或延时后分裂为三张卡，选择后可以翻面。选择背包入口卡本身保留，不隐藏。
- 背包卡按 TiltedCard 参考支持约 30° 旋转和 1.10 悬浮缩放；挂坠和零件卡没有继续叠加同等高开销的全局悬浮逻辑。

### 主题、颜色和背景

- 页面从早期黑色方案转为白色、银灰和深色文字体系；面板使用磨砂/软结构质感，但滚动内容的实时 blur 后来为了性能关闭。
- 速度、力量、技巧主题分别使用蓝青、橙红、紫色；下一步按钮、技能标签、零件局部光晕、已安装高亮会跟随当前背包连续变化。
- 零件卡内部保留黑色科技风、低对比网格和冷色光晕；外部选择区改为浅灰蓝工作台，解决黑卡片与白色面板突兀碰撞的问题。
- 底部 Dock 最终为白色玻璃质感，按钮普通态为浅色深字，悬浮态切换为深色高对比字；库按钮也遵循同样规则。
- 首页背景粒子最终使用金色并适当放大，数量控制为桌面约 180、移动端约 90；粒子层固定且不拦截鼠标。
- 左侧步骤菜单最终保持深色彩色文字；隐藏时整体向左收缩，显示后保留原位置，不因展开重新排版。

### 库页面和文字效果

- 零件库、武器库、秘宝库均改成单行数据表/记录布局，不使用三列 ChromaGrid 卡片堆叠；表格行保留局部聚光和主题边缘强调。
- 三个库的标题分别使用紫粉、蓝青、棕金主题渐变；标题和按钮主题统一，不再使用互相冲突的随机颜色。
- 库顶部按钮统一为圆角胶囊和渐变/镜面效果，悬浮时保持文字清晰；零件库额外提供单条添加、批量添加、数据库导入和导出。
- 用户提供的 GradientText、SplitText、BlurText、ChromaGrid 参考均已转为 vanilla JS/CSS 实现，项目不引入 React、Motion 或 GSAP 运行时依赖。
- 页面和区块切换增加顺序进入：技能槽、零件组、秘宝组、武器项、效果芯片、背包卡会按索引延迟出现；大标题使用逐字或渐变进入效果。
- 详细展示已经关闭在不必要的悬浮层上；最终展示页保留完整摘要、成本、秘宝和武器信息。

### 交互修复和边界规则

- 左侧轮盘曾出现滚轮回到开头，现统一通过 `syncWheelIndex()` 和 `data-current-index` 同步，禁止直接用 `updateWheel(0)` 代替步骤切换。
- 最终展示页曾出现空白，现增加异常兜底；仍必须保证编辑器和展示页使用同一个 HTTP 源、端口和 localStorage。
- 挂坠最初尝试联网物理，后来按要求移除 CDN/Rapier；当前只保留本地拖拽摆动和点击翻面。
- 武器效果必须先选择武器，最多 3 个且不重复；技能最多 3 个且不重复；零件每个技能最多 9 个且成本最多 40；秘宝最多 5 个，跨背包专属最多 1 个。
- 所有库删除均为软删除，保留旧配置引用的可恢复性；导入会校验 VERSION 5 和规则，不接受任意格式数据。

## 下一步建议

1. 补全武器的具体数据录入
2. 评估批量录入功能的必要性
3. 为每个技能上传实际图片素材
4. 用浏览器 Performance 面板定位秘宝区卡顿，再做分帧渲染
5. 清理 styles.css 中历史主题覆盖规则，合并为单一最终主题层
6. 不要重新接入联网物理挂坠，除非明确接受网络依赖和性能开销
