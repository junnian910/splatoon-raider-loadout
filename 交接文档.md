# Splatoon Raider 配装器 — 交接文档

> 本文是项目**当前状态**的完整交接记录，已按"同功能只保留最新实现"合并去重：历史覆盖层记录（v1/v2/v3 等）与旧版优化记录均已删除，被合并进各功能章节的最新描述。
> 修改前请先读「现状总览」「交互规则与已解决的坑」和「已知问题」。

## 项目概述

Splatoon 主题的游戏配装（Build）编辑器。前端为 vanilla JS 单页应用，无框架、无构建工具，直接双击 `index.html` 即可运行（图片/分享建议用 HTTP，`npx serve .`）。配装数据存浏览器 localStorage。

流程：主页入口/轮盘 → 背包 → 武器 → 配件 → 零件 → 秘宝 → 最终展示（showcase.html）。

**后端（2026-08-03 接入）**：共享零件库 + 提交审核系统。所有访客看到同一份正式库；访客提交的零件进入待审核区，管理员在 admin.html 审核通过后才公开。Cloudflare Workers + D1，正式域名 `https://www.jun910.top`。

**2026-08-05 大批视觉/功能修复**：挂坠拖拽、武器弹窗乱码、配件选卡渐变、深蓝徽标彩色流动、最终展示页整体重做（含锁链物理折叠动画）。详见文末「2026-08-05 本轮改动记录」。

**2026-08-06 零件库在线化改造**：编辑/删除/批量全部走服务器待审核 + 修改历史（详情按钮）、弹窗主题色、上传裁剪（手机选头像同款手势）、品质五角星、移除导入导出。详见文末「2026-08-06 本轮改动记录」。

## 目录与页面入口

| 文件/目录 | 用途 |
|---|---|
| `index.html` | 主页（片头→home 两阶段）+ 配装编辑器五步 |
| `app.js` | 主编辑器渲染、选择交互、主题同步、分享/导入、动画、主页特效（~1500 行） |
| `core.js` | 数据存储、数据规范化、规则校验、分享编码、DOM 工具 `el()`、确认弹窗、Canvas 粒子 |
| `data.js` | VERSION 5 种子数据、背包、技能、品质边框、秘宝、武器类型和默认配置 |
| `lanyard-physics.js` | 挂坠纯 JS Verlet 绳索物理（无 CDN/Rapier） |
| `library.html/js` | 零件库：列表读共享正式库；添加/编辑/删除/批量均**提交待审核**；详情查看修改历史；上传裁剪 |
| `weapon-library.html/js` | 武器类型和武器记录管理、图片压缩、软删除 |
| `treasure-library.html/js` | 秘宝记录管理、普通/背包归属、图片压缩、软删除 |
| `showcase.html/js` | 最终配装展示（读同源 localStorage） |
| `admin.html/js` | 管理员审核后台：登录、待审核列表、通过/拒绝 |
| `mobile-menu.js` | 手机端汉堡菜单（5 页共用，从现有 DOM 生成内容） |
| `worker.js` | Worker 入口：`/api/*` 手动分发，其余走 `env.ASSETS` |
| `_auth.js` / `_plugins.js` | 后端鉴权（HMAC token/cookie）/ 零件校验与 quality 映射 |
| `schema.sql` / `seed-data.sql` / `tools-seed-db.js` | D1 建表 / 927 条初始零件 SQL / 生成脚本 |
| `wrangler.jsonc` | Worker + assets + D1 + 自定义域名路由 |
| `styles.css` | 全站样式（末尾追加覆盖层模式，已 >3700 行） |
| `tests/rules.test.js` | 核心规则单元测试 |
| `splatoon-raider-catalog-v5.json` | 数据库导出备份（不自动替代浏览器缓存） |

图片素材：`背包照片/`（首次展示图、三背包正反面）、`零件外观图片/`（5 张品质边框，512×512）、`插件图片/`、`挂坠图片/`（正面.jpg/反面.jpg）、`初始卡片图片/`（主页 6 卡 SVG）、`GB7.ttf`/`GB9.ttf`（游戏字体）、`效果文件/`（设计参考 txt，gitignore 不部署）。

## 现状总览

- 数据版本 VERSION 5；运行时 catalog/build 存 localStorage（`splatoon-raider-v5-catalog` / `splatoon-raider-v5-build`）。
- 视觉为白色/银灰磨砂玻璃体系，深色文字 + 背包主题色（速度蓝青/力量橙红/技巧紫）。
- 页面切换有顺序入场动画；手机端（≤720px）用汉堡菜单替代轮盘和 Dock。
- 本次已修复：挂坠拖拽失效（图片原生拖拽吞事件）、武器弹窗乱码（`replaceChildren` 未展开）、配件选卡样式、展示页 7 项改造 + 锁链物理折叠。
- 当前最大遗留问题：秘宝/零件等大量卡片切换页面时的短暂卡顿（分帧 DOM 未做）；styles.css 历史覆盖层待合并。

## 核心功能与当前实现

### 1. 主页（index.html）

- 片头两阶段：`body[data-home-stage="intro"]` 居中大标题 + click 按钮 → 点击播放序列（标题归位 → 副标题逐字 → 6 卡逐张升起 → 卡片翻面 → **电脑端**名称飞入左侧轮盘 → 轮盘收回 → 挂坠坠落 → 描述重现 → Dock 冒出）。手机端跳过飞字。
- 片头标记 `raiderIntroPlayed`（localStorage，回退 sessionStorage）；`?intro=1` 强制重播；片头期间 `body.is-sequence-locked` 锁滚动。
- 6 张 bento 卡（`.bento-face` **必须有背景**，否则透明），跨页动画：翻面 → 图标飞向视口左上角固定落点 (8,12)（与 `.panel-corner-icon` 重合）→ 面板缓入。
- 背景 Canvas 金色粒子（桌面 180/移动 90，约 60fps，页面隐藏暂停）；手机端粒子自动上飘替代鼠标 hover。
- 左侧弧形轮盘（`#stepWheel`，参数：字号 3rem、间距 1.4、curve 1、tilt 6deg、blur 2px、fade 0.25、smoothing 200ms、inset 80px）：`syncWheelIndex(stepId)` 是唯一状态同步入口，滚轮索引存 `data-current-index`。隐藏时用 visibility/opacity 控制，不删除节点。
- 底部 Dock（全站）：默认只显图标字母，悬浮显示标签。

### 2. 配装编辑器（5 步骤）

`showPanelOnly()` 控制单步显示；`data-next` 与轮盘 `data-step` 同一套步骤 ID。公共能力：选择即存 build、导出分享码/JSON、导入校验（版本/标识/大小/规则）、重置、Toast 与确认弹窗、软删除。

**背包**：三张卡（桌面 330×440 / 手机 120×175 横滑）。大图 → 2.5s 或移入分裂为三卡 → 点击翻面选中（`build.mainPackId`）→ 切换主题色。TiltedCard 倾斜 ±30°、悬浮 1.10。选中后**不整体重渲染**，只更新依赖项。查看大图按钮不改变已保存选择（`packPreviewMode`）。卡片底部名称用渐变流动文字（`.pack-card-label--speed/power/technique` + `gradient-scroll` 动画）。

**武器**：弹窗内左右切类型（`#weaponPrev/Next`）+ 下方武器列表。选中写 `build.weaponId`；效果最多 3 个且不重复，无武器时禁用。弹窗为浅色磨砂玻璃。⚠️ 2026-08-05 修复：武器列表渲染必须 `replaceChildren(...(...))` 展开数组，否则出现 `[object HTMLButtonElement]` 乱码。

**配件/技能**：三个背包列各 5 张选卡（`.picker-skill`）。当前实现：
- 弹窗背景 `.skill-picker` 为**淡蓝→淡绿渐变**（`linear-gradient(150deg,#e7f4fb,#dcf2e8,#e3f0f9)`），列头圆徽标为**彩色流动渐变**（`badge-color-flow`，紫→粉→蓝→青）。
- 选卡**按背包上色**：速度淡蓝 / 力量淡橙 / 技巧淡紫（`data-pack` + `--picker-accent`），默认即带浅色渐变底 + 主题色描边。
- hover：上浮 2px + 主题色描边 + 柔光 + 主题色文字；选中：主题色双圈描边。
- 规则：同一技能不重复，最多 3 个；未选主背包时先提示。

**零件**：按技能归属 + 同名分组互斥切换；排序 彩虹→金→紫→绿→白，同品质按成本降序。桌面一行 8 个、移动 4 个。DOM 结构 `plugin-tile-button` → 品质边框图 → 零件图 → 成本角标（**完整圆贴图片外框左上角内侧**，最终规则 `top:12px;left:12px;transform:none`；已安装缩略图/展示页为 `top:2px;left:2px`）。成本 ≤40、插槽 ≤9。悬浮提示只显示加成数值。已安装用主题色单边框 + 右下角"已选"角标。零件卡内部黑科技风，外部浅灰蓝工作台。

**秘宝**：普通/背包专属分组；最多 5 个；跨背包专属最多 1 个（`rules.canInstallTreasure`）。按归属配色：普通淡绿 / 速度淡蓝 / 力量淡橙 / 技巧淡紫（`data-scope`/`data-pack`）。点击已装项移除，不可用项提示原因。

### 3. 数据管理页面

- 零件库：共享正式库为列表数据源（`fetch('/api/plugins')`，失败回退本地 catalog）；**添加/编辑/删除/批量添加/批量编辑/批量删除全部提交服务器待审核**（2026-08-06 起，本地离线模式接口失败时仍回退 localStorage 直改）。
- 三库共用：白磨砂玻璃、标题渐变（零件库紫粉 / 武器库蓝青 / 秘宝库暖金）、顶部胶囊按钮紫粉蓝青渐变、表格行聚光、软删除 + 恢复、图片 Canvas 压缩到 ≤512px WebP。（零件库的"保存/导入数据库文件"已随在线化移除。）
- 编辑器读 localStorage，不自动读根目录 JSON；换库需导入或清缓存重载种子。

### 4. 主题与视觉

- 白色/银灰磨砂主题，CSS 变量集中在 `styles.css` 的第二个 `:root`（**有两个 :root，第二个全量覆盖第一个，改颜色改第二个或追加文件末尾**）。
- `--ink` / `--ink-soft` / `--surface` / `--line` / `--theme-accent(-soft/glow)`。
- 深蓝徽标统一为**彩色流动渐变**（`.badge-color-flow` 动画，紫→粉→蓝→青，`background-size:300% 100%`）：技能弹窗标题栏、技能列徽标、武器类型标签、技能槽加号、数字徽标。
- 性能约束：滚动内容关闭 backdrop-filter（保留固定头部/弹窗/Dock 的模糊）、卡片 `contain: layout paint`、粒子数量受限、库标题渐变周期放慢。**styles.css 末尾的最终覆盖层必须保留语义，不要只删"看起来重复"的规则**。

### 5. 数据模型（VERSION 5）

- `build.mainPackId` / `build.packStates`（每背包独立状态）/ `build.skillIds`（≤3）/ `build.pluginsBySkillId`（≤9、成本 ≤40）/ `build.treasureIds`（≤5）/ `build.weaponId` / `build.weaponEffectIds`（≤3）。
- catalog 记录均支持 `deletedAt`；品质字段存 `seed.qualityBorders` 图片路径（前端），后端 DB 存枚举 key 再映射回路径。
- `normalizeCatalog()` / `normalizeBuild()` 负责补全/规范化旧数据。**不要改 VERSION 5**，否则会回退种子/清空用户数据。

### 6. 最终展示页（showcase.html）

2026-08-05 整体重做，当前实现：

- 标题：只有居中"最终配装"，**无 03/英文代码块、无"配置规则检查通过"状态行**（`#showcaseStatus` 元素已移除，JS 中用 `?.` 兜底）。
- 三张技能卡：零件列表**不用滚轮**，全部直接显示。
- 零件行：缩略图 = 品质边框 + 零件图 + **成本角标在图片边框左上角**（`.showcase-cost`）；中间名称/效果；**右侧显示数值加成**（`bonusText`，如 `+25%`）。
- 零件折叠动画（锁链物理）：初始零件列表像**一正一反的 accordion 折堆**藏在配件标题背景后（每张卡交替左右偏 + 交替倾斜，越往下弯曲越大，只露最后一角的右下角）；点击卡片区域立即落下，**5 秒后自动落下**；落下用**锁链式 Verlet 物理**（每张卡是链节：槽位弹簧 + 相邻距离约束 + 每节独立相位摆动，每个摆动都不一样），落定后自动清空 transform。实现：`showcase.js` 的 `foldCards()` / `dropFold()`；无零件直接展开；彩色"零件 ▾"角标已隐藏（`display:none`），点击卡片区域仍可触发。
- 背包摘要：`MAIN BACKPACK` 标签 + 背包照片（150×150，透明框无黑底）+ 照片下方**渐变流动文字**（如"速度背包"，与背包选择步骤同款 `pack-card-label--{id}`）；卡片背景为主题色渐变（速度淡蓝/力量淡橙/技巧淡紫）。
- 秘宝摘要：标签只显示 `TREASURES`（**无 5/5**）；卡片按归属配色（普通绿/速度蓝/力量橙/技巧紫）。
- 武器摘要不变。渲染异常兜底提示保留。读取同源 localStorage，`file://` 与 HTTP 混用会显示为空。

### 7. 挂坠（lanyard-physics.js）

2026-08-05 修复拖拽后当前实现：

- 纯 JS Verlet 绳索（无 CDN/Rapier/WASM），canvas 只画绳子+顶环，卡片为 DOM（正反两张 `<img>` + rotateY 翻面）。
- **widget 尺寸为 `min(100vw,2000px) × 100vh`**（不可见、`pointer-events:none`），canvas 铺满 → 绳子任意方向拖拽都不会被画布边界切掉。
- 卡片锚定在 widget 右侧：CSS 变量 `--lanyard-card-offset` / `--lanyard-card-w`（桌面 120/220，≤900px 74/132，≤720px 58/100），JS `cardOffset` 从 CSS 变量读取，锚点在 `W - cardOffset`。
- **交互全部在卡片上**：`pointerdown` 绑 card，`pointermove/up` 绑 document（不用 `setPointerCapture`）；canvas `pointer-events:none` 不拦截其他按钮点击。
- ⚠️ **图片必须 `draggable="false"` 且 `.lanyard-face { pointer-events:none; -webkit-user-drag:none }`**：否则按住图片拖动会触发浏览器原生图片拖拽（dragstart/pointercancel），吞掉整个拖拽和翻面——这是 2026-08-05 电脑端拖拽失效的真凶。
- 非物理态用 click 翻面（`onCardClick` 检查 `is-physics-ready`）；物理态 `onPointerUp` 短按翻面（拖过 4px 抑制）。
- 只在主页显示；进入 builder 步骤 `hidden=true` 停 rAF，回主页恢复。
- 手机端不缩高度（绳子物理链完整），只缩卡片宽度。

### 8. 共享零件库与审核系统（后端）

架构：访客 `GET /api/plugins` 读正式库、`POST /api/plugins/submit` 提交待审核；管理员登录后 `GET /api/admin/pending` + approve/reject。

| 方法 路径 | 作用 | 权限 |
|---|---|---|
| `GET /api/plugins` | 返回 approved 且未软删的零件 | 所有人 |
| `POST /api/plugins/submit` | 提交新零件（pending，action=create） | 所有人 |
| `POST /api/plugins/update` | 提交修改请求（pending，action=update，origin_id=原id） | 所有人 |
| `POST /api/plugins/delete` | 提交删除请求（pending，action=delete，内容拷贝原零件） | 所有人 |
| `GET /api/plugins/history?pluginId=` | 零件修改历史（公开） | 所有人 |
| `POST /api/admin/login` / `logout` | 登录（HMAC token，HttpOnly cookie 7 天） | 所有人 |
| `GET /api/admin/pending` | 待审核列表（含 action/originId） | 仅管理员 |
| `POST /api/admin/approve` / `reject` | 审核；**approve 按 action 分支**：create→通过+写历史；update→后端 diff 后写回原行、删请求行、写历史（原零件已删则自动拒绝）；delete→软删原行、写历史 | 仅管理员 |

- 密码 `ADMIN_PASSWORD`、token 密钥 `TOKEN_SECRET` 存 Cloudflare Secret，不进代码/不进 git；`.dev.vars` 供本地（已 gitignore）。
- 后端独立校验（cost 非负整数、name≤40、effect≤120、bonus≤30、image≤5MB base64、quality 枚举、skillId 合法），不能信前端。
- 数据库：`splatoon-raider-db`，表 `plugins`（id/skill_id/name/slot_cost/quality 枚举/effect_text/bonus_text/image/status/submitted_by/created_at/reviewed_at/deleted_at/**action** create|update|delete/**origin_id**），索引 idx_plugins_status、idx_plugins_skill；另有 **`plugin_history`**（id/plugin_id/action/changed_by/changes JSON/created_at，索引 idx_plugin_history_plugin）记录每次创建/修改/删除（谁、何时、改了哪些字段）。初始 927 条 approved 数据（`seed-data.sql` 导入）。

### 9. 手机端适配（≤720px）

- 汉堡菜单（`mobile-menu.js`）替换轮盘 + Dock；右上角按钮展开 3 张卡：配装步骤 / 导航 / 数据。内容从现有 DOM 生成，与桌面导航同步。侧滑返回（History API）回上一步。
- 库表格保留表格 + 横向滚动 + "← 左右滑动查看更多 →" 提示。
- 片头无飞字；背景光晕漂移、粒子自动上飘、卡片边框渐变流动；鼠标聚光/涟漪/视差禁用。

## 数据保存说明与迁移

- 运行时 catalog 存 localStorage；库页"保存数据库文件/从文件导入"已于 2026-08-06 移除（上线后以服务器为准，不再需要本地备份文件）。
- 修改前先备份 v5 JSON；只恢复配装用主页"导入"build 分享码，不要导入整个数据库。
- 不能把 JSON 改名覆盖 `data.js`；种子与运行时 catalog 是两层来源。

## 已知问题 / 待办

1. 秘宝/零件等大量卡片切换页面仍可能短暂卡顿 → 下一步做分帧 DOM 创建或虚拟化，不要继续加动画。
2. `styles.css` 历史覆盖层仍多，功能稳定后可做无视觉变化的 token 合并（保留末尾最终覆盖规则语义）。
3. 武器/秘宝库数据录入已有；零件批量录入待评估重新集成。
4. 真实浏览器 Performance 面板验证未完成（Node 语法检查不能替代 FPS/长任务检查）。
5. 手机端主页再侧滑会退出网站（系统行为，无法完全阻止）。
6. 每个技能的实际图片素材待补充。
7. **2026-08-06 改造待验证**：代码已实现并部署，但用户反馈"未见效果"——优先怀疑浏览器/CDN 缓存，需强刷（Ctrl+F5/无痕）逐项验收；裁剪双指捏合需真机触控验证；admin 审核 update/delete 完整链路（通过/拒绝）未实操验证。

## 绝对不要踩的坑

1. **`replaceChildren(...arr)` 必须展开**：`el.replaceChildren(arr)` 会把数组转字符串 → `[object HTMLButtonElement]` 乱码。2026-08-05 武器弹窗乱码即此因。
2. **`img` 原生拖拽吞指针事件**：挂坠等可拖拽交互里的图片必须 `draggable="false"` + `pointer-events:none`，否则按住拖动触发原生 drag → pointercancel，拖拽/点击全失效。
3. **铺满视口的 canvas 必须 `pointer-events:none`**：交互绑定到真实元素（卡片），否则挡掉全屏点击。
4. `button` 内不能嵌套 `div`（浏览器 DOM 修正导致乱码）。
5. `file://` 协议 WebGL/Canvas 无法加载本地图片（CORS），必须 HTTP（`npx serve .`）。
6. `app.js` 主 IIFE 很长（~1500 行），括号匹配易错，改完 `node --check`。
7. `el()` 的 text 参数必须传字符串。
8. VERSION 升级 = 清空数据。
9. **D1 写接口必须 `.run()`、读接口 `.all()/.first()`**：漏写会"返回成功但没入库"。
10. **Workers assets 模式不认 `functions/` 目录**：路由全在 `worker.js` 手动分发。
11. Secret 不进 git；**每次 `wrangler deploy` 后 `wrangler secret list` 确认** `ADMIN_PASSWORD`/`TOKEN_SECRET` 还在，丢了重新 put。
12. 本地库 `--local` vs 线上库 `--remote` 不互通，测试别灌线上。
13. `mobile-nav` 壳必须在 `mobile-menu.js` 之前加载；`hidden` 属性要用 `display:block !important` 压过。
14. `.bento-face` 必须自带背景（`inset:0` 覆盖层）。
15. 两个 `:root`：第二个（亮色磨砂）覆盖第一个，改颜色改第二个或追加文件末尾。
16. 无重力 Verlet 弹簧模型（showcase 折叠）能避免静力下垂：重力 + 弹簧会产生静止偏移导致永不收敛。
17. 跨页动画先翻面（0.6s）再飞图标，否则翻转被抢走看不见。

## 文件结构

```
├── index.html / app.js / core.js / data.js / lanyard-physics.js / mobile-menu.js
├── library.html/js、weapon-library.html/js、treasure-library.html/js（数据管理）
├── showcase.html/js（最终展示）、admin.html/js（审核后台）
├── worker.js / _auth.js / _plugins.js（后端）
├── plugin-data.js（882 条导入数据）、schema.sql、seed-data.sql、tools-seed-db.js
├── wrangler.jsonc、.dev.vars（本地 Secret，gitignore）、.assetsignore
├── styles.css（>3700 行，末尾追加覆盖层模式）
├── tests/rules.test.js
├── 背包照片/ 零件外观图片/ 插件图片/ 挂坠图片/ 初始卡片图片/ 效果文件/（参考，不部署）
└── GB7.ttf / GB9.ttf
```

## 如何运行

```bash
npx serve .            # 本地开发（HTTP，图片/localStorage 稳定）
node tests/rules.test.js   # 规则测试
node --check app.js core.js showcase.js lanyard-physics.js  # 语法检查

npx wrangler dev                       # 后端本地（需 .dev.vars + 本地库）
npx wrangler d1 execute splatoon-raider-db --local --file=schema.sql
npx wrangler d1 execute splatoon-raider-db --local --file=seed-data.sql
npx wrangler deploy                    # 部署到 www.jun910.top（不依赖 git push）
npx wrangler secret put ADMIN_PASSWORD / TOKEN_SECRET
npx wrangler secret list               # 部署后确认 Secret 还在
npx wrangler d1 execute splatoon-raider-db --remote --command="SELECT status, COUNT(*) FROM plugins GROUP BY status;"
```

- git push 只做 GitHub 备份；线上以 `wrangler deploy` 为准。Cloudflare 自动部署已改 Worker 模式。
- 网络：站长在国内，github/workers.dev 受限，git 已配代理 `http://127.0.0.1:10808`（推送前开代理）；`*.workers.dev` 临时域名国内打不开，验证用 `jun910.top`；curl 超时常是本地网络问题，用浏览器验证。
- 部署后登录失败第一步查 `wrangler secret list`。

## 推荐验收清单

- 同一 HTTP 源打开 index/showcase，选完五步后看最终展示：零件成本角标在左上、右侧加成、秘宝配色、背包照片+渐变文字。
- 展示页零件列表初始折叠（露小角），点击卡片区域或 5 秒后锁链式落下，每节摆动不同。
- 配件弹窗：淡蓝淡绿渐变背景、选卡按背包上色、hover 上浮发光；列徽标/标题栏彩色流动。
- 武器弹窗：切换类型 + 点选武器正常，无 `[object HTMLButtonElement]` 乱码。
- 主页挂坠：拖动翻面正常，绳子不被切、不挡其他按钮（桌面 + 手机各测）。
- 零件同名互斥、品质排序、成本 ≤40/插槽 ≤9；秘宝 ≤5、跨背包 ≤1；武器效果 ≤3 且无武器禁用。
- 控制台无脚本错误；`node --check` + 规则测试通过。

---

## 2026-08-05 本轮改动记录（挂坠/武器弹窗/配件选卡/展示页）

### 一、挂坠拖拽修复（电脑端 + 手机端）

**现象**：拖拽无效果（电脑端）；绳子被"白色背景"切断；widget 变大后挡住其他按钮。

**根因链**：
1. 绳子被切 = canvas 只有 widget 大小，卡片拖出画布后绳子被边界裁掉 → **widget 改为 `min(100vw,2000px) × 100vh`**（不可见容器），canvas 铺满，任意方向拖拽都能画下。
2. 挡住其他按钮 = canvas `pointer-events:auto` 铺满视口 → **canvas 改 `pointer-events:none`**，交互移到卡片。
3. 电脑端拖拽失效 = 事件绑到卡片后，指针落在卡片里的 `<img>` 上，**img 原生可拖拽**，按住拖动触发浏览器原生 drag → pointercancel → 拖拽/翻面全失效 → **img 加 `draggable="false"` + `.lanyard-face{pointer-events:none;-webkit-user-drag:none}`**。之前能用是因为旧版事件绑在 canvas（z-index 高于卡片），指针永远落不到 img 上。
4. 事件绑定最终方案：`pointerdown` 绑 card，`pointermove/up/cancel` 绑 document（不用 `setPointerCapture`，button 上不稳定）；`onPointerDown` 检查 `is-physics-ready`；非物理态用 click 翻面。
5. 卡片锚定 widget 右侧：CSS 变量 `--lanyard-card-offset`/`--lanyard-card-w`（120/220、74/132、58/100 三档），JS 从变量读 cardOffset。
6. 删除了 styles.css 文件末尾的旧移动端覆盖层（强制 widget 70px/卡片 70px），它静默覆盖所有改动。

### 二、武器弹窗乱码修复

**现象**：打开武器选择，武器列表区域显示 `[object HTMLButtonElement]`。

**根因**：`app.js renderWeaponPicker()` 里 `weaponListGrid.replaceChildren(array)` **漏了展开运算符**（历史遗留，git HEAD 里就存在）。修复为 `replaceChildren(...(map(...)))`。已排查其他 replaceChildren 调用点均正常。

### 三、配件选卡重做 + 深蓝徽标彩色流动

- 弹窗背景 `#skillDialog .skill-picker`：淡蓝→淡绿渐变（`linear-gradient(150deg,#e7f4fb,#dcf2e8,#e3f0f9)`）。
- 选卡按背包上色：速度淡蓝/力量淡橙/技巧淡紫（`[data-pack]` + `--picker-accent`），默认浅色渐变底 + 主题描边；hover 上浮 2px + 描边发光 + 主题色文字；选中双圈描边。
- `badge-color-flow` 关键帧（紫→粉→蓝→青，`background-size:300% 100%`）应用于：技能弹窗标题栏（6s）、技能列徽标、武器类型标签、技能槽加号、数字徽标（5s）。
- 零件库"静态模式"弹窗（plugin-editor-open）保持无动画，未动。

### 四、最终展示页重做（7 项）

1. 标题去掉右侧 03/RAIDER 英文块和 overline，去掉"配置规则检查通过"状态行（HTML 移除元素，JS `?.` 兜底）。
2. 零件列表取消滚轮（`max-height:none; overflow:visible`），全部显示。
3. 背包：`MAIN BACKPACK` 标签 + 照片（150×150 透明框）+ 照片下渐变文字"速度背包"（复用 `pack-card-label--{id}`），卡片背景为主题色渐变。
4. 秘宝：标签 `TREASURES` 无 5/5；卡片按归属配色（普通绿/速度蓝/力量橙/技巧紫）。
5. 零件行：成本角标在图片边框左上角（`.showcase-cost`），右侧显示数值加成（原为成本）。
6. 锁链物理折叠：`foldCards()` 一正一反 accordion 折堆（交替偏移/旋转，弯曲渐变，只露右下角）藏到配件标题后；`dropFold()` Verlet 链（槽位弹簧 + 相邻距离约束 + 每节独立相位摆动）落下；点击卡片区域或 5 秒后触发；落定清 transform。彩色"零件 ▾"角标已隐藏（点击卡片区域仍有效）。
7. 无重力弹簧模型：避免重力+弹簧产生静力下垂（偏移永远收敛不了）。

**文件**：index.html（img draggable）、app.js（lanyard 事件移除、武器弹窗展开修复）、lanyard-physics.js（事件/锚点）、showcase.html（标题）、showcase.js（折叠物理 + 展示重做）、styles.css（挂坠/选卡/流动徽标/展示页覆盖层）。

---

## 2026-08-06 本轮改动记录（零件库在线化改造）

### 背景

上线后零件库列表数据来自 `GET /api/plugins`（sharedPlugins），但编辑/删除/批量按钮仍在操作本地 `catalog.plugins`，目标恒为空 → 批量按钮"无效"（单行编辑/删除同样失效）。用户确认方案：**编辑/删除进入待审核**（管理员通过后生效），批量添加每行也提交待审核；修改需记录"哪个用户改的"，保留多人修改历史。

### 一、数据库（schema.sql 已更新；本地 `--local` 与线上 `--remote` DDL 均已执行）

- `plugins` 表加列：`action`（create/update/delete，默认 create）、`origin_id`（修改/删除请求指向的原零件 id）。
- 新表 `plugin_history`（id/plugin_id/action/changed_by/changes JSON/created_at）+ 索引 idx_plugin_history_plugin。changes 存字段级 diff `{field:{from,to}}`。

### 二、后端（worker.js）

- 新接口：`POST /api/plugins/update`（校验后插入 pending 行，action=update、origin_id=原id）、`POST /api/plugins/delete`（pending 行拷贝原零件内容，action=delete）、`GET /api/plugins/history?pluginId=`（公开）。
- `handlePending` 返回 action/originId；`handleApprove` 按 action 分支：
  - create：原逻辑通过 + 写"创建"历史；
  - update：后端对比原行自动 diff，写回原行（skill/name/cost/quality/effect/bonus/image），删请求行，写"修改"历史；原零件已删则自动拒绝；
  - delete：软删原行（deleted_at），删请求行，写"删除"历史。
- 用户标识：前端首次操作弹 window.prompt 昵称，存 localStorage `raider.nickname`（可空→匿名），随 submit/update/delete 提交。

### 三、前端（library.html/js、admin.html/js、styles.css）

1. **批量按钮修复**：编辑/删除/批量编辑/批量删除/批量添加全部走 API（在线时，串行逐条提交），完成后 `reloadShared()` 刷新；离线（接口失败）回退原本地逻辑。
2. **详情按钮 + 历史**：行操作列新增"详情"→ 弹窗显示当前信息 + 历史列表（创建/修改/删除徽标、昵称、时间、字段 旧→新 摘要）。
3. **弹窗主题色**：库页面（零件/武器/秘宝/admin）所有弹窗与内部卡片（品质选卡、批量行、上传预览、表单控件）改为按 `--library-a/b/c` 主题色浅色渐变（`.library-page .dialog` 作用域，不影响配装器深色弹窗）。
4. **上传裁剪**：手机选头像同款——方形取景框 + 白边/四角/三分线辅助；**双指捏合缩放**（Pointer Events 多点触控，以两指中点为锚点缩放）、单指拖动平移、桌面滚轮缩放；确认后按取景范围导出 512×512 WebP。无缩放按钮。
5. **移除导入导出**：删"保存数据库文件/从文件导入"按钮与相关代码（在线后不需要）。
6. **品质五角星**：表格品质列 = 品质边框小图 + 五角星（白1/绿2/紫3/金4/彩虹5★，颜色对应，彩虹渐变文字），库表与审核表同步。
7. **成本角标**：上传预览 `.composite-cost-badge` → `top:12px;left:12px`（与零件卡一致；零件卡角标用户自调为 12px，已安装/展示页为 2px）。

### 四、部署与验证状态

- 2026-08-06 已 `wrangler deploy`（Worker + assets），线上生效。线上已验证：`/api/plugins`、`/api/plugins/history` 返回正常，update/delete 对不存在 id 正确返回 404，各页面 200。
- **`.assetsignore` 新增 `*.glb`**：`配件图片/` 下 3 个 .glb（共约 197MB）超出 Workers assets 25MB 上限会阻断部署；.glb 文件未删除，本地 `wrangler dev` 仍从磁盘读取，仅不随线上部署上传。
- ⚠️ **用户反馈"全部没有实现"（疑浏览器/CDN 缓存）**：待强刷后逐项验收——批量按钮、详情历史、审核 update/delete 分支、双指捏合裁剪（真机）、角标位置、品质列。详见「已知问题」第 7 条。
